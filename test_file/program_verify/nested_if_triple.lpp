int x,A;
{x<1000000000&&x>0&&x==A}
//将x<1000000000则无法通过验证
if (x >= 0) {
    if (x > 3) {
        x = x - 1;
    }
    else{
        x = x - 2;
    }
    x = x + 100;
}
else{
    x = -x + 5;
}
x = x - 3;
{x>A}